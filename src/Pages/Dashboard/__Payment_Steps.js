import React from "react";

const __Payment_Steps = () => {
  return (
    <div>
      {/* 1. react stripe js install 
      2.open stripe account on stripe website 
      3.get
      publishable key pk_ 
      4.create element wrapper using publishable key
      5.create cheekout from using card element usStripe,useElements
       6.get card
      elements info(cardit card info) ---------------------------------------
      7.get creadit card info/error display error(if any)
      
      8.get client secret from backend via payment intent post api*/}
      9.store client secerit  on the client site
    </div>
  );
};

export default __Payment_Steps;
